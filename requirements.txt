# ── Core trading ─────────────────────────────────────────────────────────────
ccxt==4.3.90
pandas==2.2.2
numpy==1.26.4
ta==0.11.0
python-dotenv==1.0.1
websockets==12.0
aiohttp==3.9.5
rich==13.7.1
apscheduler==3.10.4

# ── ML — LightGBM (current strategy) ────────────────────────────────────────
lightgbm==4.4.0
scikit-learn==1.5.1

# ── ML — TFT (next strategy) ─────────────────────────────────────────────────
# Install separately due to size:
# pip install torch --index-url https://download.pytorch.org/whl/cpu
# pip install pytorch-forecasting pytorch-lightning
torch>=2.0.0
pytorch-lightning>=2.0.0
pytorch-forecasting>=1.0.0
